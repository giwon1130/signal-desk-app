# 프론트엔드 가이드

> **누가 봄**: FE 개발자 (모바일 + 웹)
> **언제 봄**: 새 화면 / 새 hook / state 추가 시
> **함께 봐야 할 문서**: [아키텍처](아키텍처.md), [데이터모델](데이터모델.md), [컴포넌트](../디자인/컴포넌트.md)

## 모바일 vs 웹 분기

(stub)
- `Platform.OS === 'web'` → 웹 전용 셸 (`src/web/*`)
- 그 외 → 모바일 탭 (`src/tabs/*`)
- 공유: `src/components/`, `src/hooks/`, `src/api/`, `src/types.ts`, `src/utils.ts`, `src/styles/`

## 상태 관리

(stub) Context 도입 안 함. 커스텀 훅으로 캡슐화.

| 훅 | 책임 |
|----|------|
| `useAuthSession` | authChecked / user / login / logout |
| `useMarketSnapshot` | summary/sections/aiRec/watchlist/portfolio/fortune/topMovers/alertHistory + refresh |
| `useStockSearch` | 종목 검색 + 디바운싱 + cancel |
| `useWorkspaceMutations` | save/delete watchlist/portfolio + optimistic update |
| `useChartSelection` | chartMarket/chartPeriod/selectedIndexLabel |
| `useLivePrices` | WS 구독 (KR 만) |
| `useToast` | 하단 알림 |
| `usePushDeepLink` | 알림 탭 시 종목 상세 모달 열기 |
| `useMarketReminder*` | 로컬 알림 + 히스토리 |

## 라우팅

(stub) React Navigation 안 씀. App.tsx 의 `activeTab` state + 조건부 렌더.

## 스타일

(stub)
- `src/styles/` 도메인별 분리 (shell/cards/today/home/market/stocks/ai/modal/util)
- `useStyles()` 진입점이 `StyleSheet.create` 한 번에 합침
- 인라인 스타일 OK (반복되면 styles/ 로 승격)

## 폼 / 입력

(stub)
- 컨트롤드 input. validation 은 onSubmit 시점.
- 키보드 회피: `KeyboardAvoidingView` (iOS) + `ScrollView`

## 에러 / 로딩

(stub)
- 에러: 카드 단위 fallback (전체 깨지지 않게).
- 로딩: Skeleton (`src/components/Skeleton.tsx`) 또는 텍스트 placeholder.

## 모달

(stub) RN `<Modal animationType="slide">`. 종목 상세 + 알림 설정 + 보유 등록 등.

## 차트

(stub)
- 모바일: `react-native-svg` 기반 (`CandleVolumeChart.tsx`)
- 웹: 인라인 SVG (`Sparkline` shared.tsx)

## 알림

(stub)
- 로컬: `expo-notifications` + WEEKLY trigger × 평일 5개 (DAILY 쓰면 토/일 울림)
- 푸시: 서버 발송 (Expo Push) — 실험 단계

## 빌드 / 배포

(stub)
- iOS: `npx expo run:ios --device <UDID> --configuration Release`
- 웹: Railway 자동 (main 머지) 또는 `railway up --service signal-desk-web --ci --detach`
- 자세한 배포: [운영/배포.md](../운영/배포.md)
