# 컴포넌트 인벤토리

> **누가 봄**: 디자이너 / FE
> **언제 봄**: 새 화면 만들기 전 (재사용 가능한지 먼저 확인)

## 공용 프리미티브 (`src/web/shared.tsx` + `src/components/`)

(stub) 한 줄씩 + 어디서 쓰는지.

| 컴포넌트 | 역할 | 위치 |
|---------|-----|------|
| `Widget` | 카드 셸 (제목 + meta + 아이콘 + body) | `web/shared.tsx` |
| `HoverRow` | hover 배경 적용된 Pressable 행 | `web/shared.tsx` |
| `EmptyState` | 빈 상태 (메시지 + 힌트) | `web/shared.tsx` |
| `Delta` | 등락률 (색 + 부호) | `web/shared.tsx` |
| `StanceTag` | BUY/SELL/HOLD 배지 | `web/shared.tsx` |
| `SessionBadge` | KR/US OPEN·CLOSED 점 | `web/shared.tsx` |
| `Sparkline` | 인라인 SVG 미니 차트 | `web/shared.tsx` |
| `CandleVolumeChart` | 큰 캔들+거래량 차트 | `components/` |
| `Toast` | 하단 알림 | `components/` |
| `CollapsibleCard` | 접히는 카드 | `components/` |

## 도메인 위젯 (`src/web/widgets/`)

(stub)
- `PortfolioWidget`, `NewsWidget`, `PicksRow`
- `SectorPerformanceWidget`, `AltSignalsWidget`, `TopMoversWidget`
- `AlertTimelineWidget`, `WatchAlertsWidget`

## 사이드바 섹션 (`src/web/sidebar_sections/`)

(stub)
- `Section` (셸), `WatchlistSection`, `PortfolioSection`, `RecentAiSection`, `FortuneMini`

## 모바일 탭 부속 (`src/tabs/{tab}_parts/`)

(stub)
- today_parts: BriefingCard / FortuneCard / SentimentCard / PicksCard / HoldingMonitor
- aitab_widgets: Card / Playbook / Scorecard
- market_parts: ChartSection / MarketSummaryMetrics / TopMoversSection / AlternativeSignalsSection / WatchAlertList / MoverRow

## 종목 상세 모달 (`src/components/stock_detail/`)

(stub)
- PriceHero / QuickStats / WatchToggle / PortfolioForm / AiContextRow

## 추가 원칙

(stub)
- 새 컴포넌트는 먼저 shared 에서 찾고, 없으면 추가 후 2회+ 재사용 시 승격
- props 는 가능한 한 적게 (palette + data 만 받게)
