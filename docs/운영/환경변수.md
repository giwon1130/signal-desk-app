# 환경변수

> **누가 봄**: 운영자 / 신규 인프라 셋업
> **언제 봄**: 새 환경 만들기 / 변수 추가 / 디버깅

## App (Expo) — `EXPO_PUBLIC_*`

(stub) 빌드 시점에 번들에 박힘. 런타임에 `process.env` 로 읽힘.

| 키 | 기본 | 의미 |
|----|------|------|
| `EXPO_PUBLIC_API_BASE_URL` | `https://signal-desk-api-production.up.railway.app` | 백엔드 주소 |
| `EXPO_PUBLIC_GOOGLE_CLIENT_ID_WEB` | (없음) | 웹 GIS 클라이언트 ID |

## API (Spring Boot)

### 필수
(stub)
| 키 | 의미 | 예시 |
|----|------|------|
| `PORT` | 서버 포트 | Railway 자동 주입 |
| `SIGNAL_DESK_STORE_MODE` | 저장 모드 | `jdbc` (운영) |
| `JDBC_DATABASE_URL` 또는 `DATABASE_URL` | Postgres 연결 | Railway Postgres 어태치 시 자동 |
| `SIGNAL_DESK_JWT_SECRET` | JWT 서명 키 | 32자+ 랜덤 (`openssl rand -base64 48`) |
| `SIGNAL_DESK_AUTH_GOOGLE_ALLOWED_AUDIENCES` | Google OAuth audience 화이트리스트 | 콤마 구분 클라이언트 ID |

### 권장
(stub)
| 키 | 의미 | 예시 |
|----|------|------|
| `SIGNAL_DESK_CORS_ALLOWED_ORIGIN_PATTERNS` | CORS 허용 패턴 | `https://signal-desk-web-production.up.railway.app` |
| `SIGNAL_DESK_JWT_EXPIRATION_HOURS` | 토큰 유효시간 | 720 (30일) |

### 부속
(stub) Postgres 변수는 Railway Postgres 어태치 시 자동 주입 (`PGHOST`, `PGUSER`, ...).

## Web (Dockerfile.web)

(stub) `EXPO_PUBLIC_API_BASE_URL` 만 빌드 ARG 로 받음. nginx 자체엔 환경변수 없음 (PORT 만 envsubst).

## 비밀 관리

(stub)
- **하드코딩 금지** — 모두 환경변수
- **로그에 안 찍기** — 토큰/secret/비밀번호
- **rotation 절차** — [보안.md](보안.md) 참고

## 운영 환경 외 (개발/스테이징)

(stub) 현재 production 만. 스테이징 추가 시:
- `SIGNAL_DESK_*_STAGING` prefix 또는 별도 Railway environment
- CORS 패턴에 staging 도메인 추가
