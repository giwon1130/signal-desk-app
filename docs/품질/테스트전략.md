# 테스트 전략

> **누가 봄**: QA / 모든 개발자
> **언제 봄**: 새 피처 / 릴리즈 전 / 장애 후

## 분류

(stub)
| 종류 | 도구 | 위치 | 빈도 |
|------|------|------|------|
| **단위** | Jest (FE) / JUnit5 (BE) | `*.test.ts(x)` / `*Test.kt` | PR 마다 |
| **통합** | (없음 — 추후 MockMvc) | | 릴리즈 전 |
| **수동** | 체크리스트 | 아래 | 큰 변경 시 |

## 현재 커버

(stub)
### App
- `src/__tests__/utils.test.ts` — formatPrice / formatRelativeOrShortTime / formatSignedRate / normalizeText 등 20개

### API
- `MarketSessionServiceTest` — KR/US 세션 + 휴장 분기
- `NewsSentimentBuilderTest` — 긍정/부정/중립 + publishedAt
- `RecommendationMetricsCalculatorTest`
- `WatchAlertServiceTest`
- `DailyBriefBuilderTest`
- `PersonalContextAnnotatorTest`
- `IndexChartFactoryTest`
- `PickNewsMatcherTest`

## 비커버 (TODO)

(stub)
- `MarketOverviewService` (큰 service — 외부 호출 mock 필요)
- `WorkspaceEnrichmentService` (mutation 핸들러)
- `AuthService` (login/signup/oauth)
- App: 컴포넌트 렌더 테스트 (`@testing-library/react-native` 셋업 필요)

## 실행

```bash
# App
cd signal-desk-app
npm test

# API
cd signal-desk-api
./gradlew test
```

## 수동 체크리스트 (릴리즈 전)

(stub)
### 인증
- [ ] 신규 가입 → 자동 로그인
- [ ] 이메일 로그인
- [ ] Google 로그인 (web + iOS)
- [ ] 토큰 만료 후 자동 정리

### 시장 데이터
- [ ] 정규장 시간대 — 가격 라이브 갱신
- [ ] 휴장 시간대 — Naver fetch 안 함 (로그 확인)
- [ ] 외부 API 죽었을 때 fallback 카드

### 워크스페이스
- [ ] 관심종목 추가/해제/일괄 해제
- [ ] 보유 등록/수정/삭제
- [ ] 종목 상세에서 인라인 폼

### 알림
- [ ] 평일 알림만 발화 (토/일 안 울림)
- [ ] 알림 히스토리 모달 표시

### 차트
- [ ] D/W/M 토글
- [ ] KR/US 토글
- [ ] 캔들 탭 → OHLC 툴팁

### 비주얼
- [ ] 다크모드 색상 정상
- [ ] 휴장 배너 (주말 노랑 / 공휴일 빨강)
- [ ] 통화 prefix (₩ / $)

### 보안
- [ ] CORS — 외부 도메인 403
- [ ] Rate limit — /auth 6번 째 429
- [ ] JWT 만료 시 자동 정리

## 자동화 후보

(stub)
- API smoke test (MockMvc 로 핵심 endpoint 한 번씩)
- App E2E (Maestro / Detox)
- 휴장 모드 시각 가드 — 시간 mock test
